---
schema_version: 2
log_id: ilog-20260721T155412182667Z-7c9d147c
created_at: 2026-07-21T15:54:12Z
task_type: audit
status: completed
task_id: MD-OPS-001 (napsterX/ManyDoors#232)
session_id: 46944365-723b-44fb-a6a9-d5b9415b7a5a
prompt_id: f77dfeda-0fc0-48d2-8324-0a9f53e803d9
repository: /Users/manish/git/DotFiles
branch: master
head_before: acd2da51e0d3299a3d75cdbb8078f5e7e453019b
head_after: acd2da51e0d3299a3d75cdbb8078f5e7e453019b
validation_status: passed
issue_state: open
audit_verdict: changes_required
---

# Implementation Log: Independent shipment audit of the Manydoors Repository Verification V1 adapter

## Objective

Independently determine whether the Manydoors Repository Verification Standard V1 adapter (`./scripts/verify`) is safe, truthful, deterministic, complete, standard-aligned, and ready for shipment, by reproducing its claims rather than trusting the implementation report.
Audited artifact: worktree `/private/tmp/manydoors-repository-verification-v1`, branch `feat/repository-verification-v1`, HEAD `c5ee98b`, base `origin/main @ e06db1f`.
This log is stored in DotFiles; no Manydoors file was modified.

## Scope

### Included

- Canonical-base ancestry, diff purity, registry parse, all four profiles run directly, conformance suite, orchestrator security, describe/doctor, production build + placeholder-key bundle search, gitleaks allowlist + fail-closed, npm-audit advisory classification, eval-evidence and migration-ledger advisories, ship governance, native issue #232 / epic #30, GitHub workflow + actionlint, objective/subjective boundary, docs + impl log.

### Excluded

- No modification, commit, push, PR, merge, or deploy. Live GitHub Actions run not executed (assessed statically + local-equivalent). Frozen-art SHA-256 byte proofs and the art-dependent `scripts/living-atlas/*.test.ts` (require the git-ignored working art tree) not executed.

## Repository and Task State

- **Repository state:** Audited worktree clean (tracked); only git-ignored build artifacts (`.next`, `data`, `node_modules`, `tsconfig.tsbuildinfo`). Active checkout `/Users/manish/git/ManyDoors` byte-identical before and after (main @ `e06db1f`, pre-existing bookkeeping untouched).
- **Branch:** audited `feat/repository-verification-v1` (this log: DotFiles `master`).
- **HEAD:** audited `c5ee98bda47e140f491695fe7cce86e046e4801a`.
- **Task/issue:** MD-OPS-001, napsterX/ManyDoors#232 "[MD-OPS-001] Repository Verification V1 adapter"; parent epic #30.
- **Issue state:** open
- **Issue action:** unchanged (read-only)

## Work Performed

- **VERIFIED** — Fetched origin; confirmed `origin/main == e06db1f` (recorded base) is a direct ancestor of HEAD (branch is exactly 1 commit ahead); diff is only the 8 expected files with no `src`/`app`/`public`/migration/deploy changes and no `package-lock.json` drift.
- **VERIFIED** — Parsed `CHECK_REGISTRY` independently: 17 unique checks, 14 blocking + 3 advisory; fast=6, full=11, ship=17, github-minimal=7; `describe --json` and `_plan` match exactly; fast⊂full⊂ship and github-minimal⊂full hold.
- **VERIFIED** — Ran conformance suite (23/23), fast (6/6, exit 0), github-minimal (7/7, exit 0), full (8 pass + 3 advisory, exit 0), and `ship --base origin/main` (14 blocking pass, 0 fail, 0 unavailable, 3 advisory, 17/17 executed, exit 0).
- **VERIFIED** — Production build output contains the synthetic placeholder key nowhere in `.next` (client or server); gitleaks allowlist is narrow (planted secrets in non-allowlisted source are detected, exit 1); gitleaks/npm-missing yields `depAvailable=false` (fail-closed exit-4 path); `checkMigrationDrift` deterministically detects a planted unregistered `.sql`.

## Files Changed or Inspected

| Path | State | Purpose |
|---|---|---|
| `scripts/verify.mjs` | inspected | Registry-driven orchestrator; exit engine; ship governance |
| `scripts/verify`, `scripts/verify.test.mjs` | inspected | Launcher (execs node); 23-test conformance suite |
| `.gitleaks.toml`, `.github/workflows/ci.yml` | inspected | Allowlist (4 fixtures + generated dirs); first CI (github-minimal) |
| `docs/verification.md`, impl log | inspected | Operator docs; task record |
| `src/cli/{validate,map-art-proof}.ts`, `src/llm/index.ts` | inspected | Content/asset validators; key presence check |
| DotFiles `.../repository-verification/{v1-specification,audits/manydoors-baseline,pilot-comparison}.md` | inspected | Normative standard + repo baseline |

## Decisions and Invariants

- **VERIFIED** — Single authoritative `CHECK_REGISTRY` drives describe/execution (no drift). Engine increments `executed` in every branch and reconciles `executed==planned` before any success; incomplete/duplicate-id runs return 3, never 0. Exit precedence 3>5>1>4>0; invalid invocation returns 2.
- **VERIFIED** — Child processes use argv arrays (no `shell:true`) with `stdin:'ignore'`; git calls are read-only (no reset/clean/checkout/push/commit/rm/stash); the placeholder key is read from `process.env` at runtime, never inlined, never behind `NEXT_PUBLIC`.
- **VERIFIED** — Objective/subjective boundary is truthful: no automated kid-worthiness/visual/accessibility claim; docs and describe do not claim WCAG/keyboard/browser/visual-regression coverage.
- **SESSION EVIDENCE** — Real check timeouts throw and are classified exit 3 (adapter error), not exit 5; the conformance `timeout->5` assertion exercises a stub no production check emits (see D-04).

## Audit Verdict

- **Verdict:** CHANGES REQUIRED
- **Decision basis:** No P0/P1; the clean HEAD ships green with a complete reconciled run. But three checks the Manydoors baseline prescribes as blocking (evidence-integrity, SCA/npm-audit, Drizzle migration-drift) are wholesale-advisory; two currently fail on pre-existing debt, so advisory status is load-bearing for a green ship and leaves those domains with no blocking regression gate (D-01, P2). Standard section 12 (generated-consistency MUST block) is unmet by any blocking check (D-02, P2). Git-ignored production art makes a bare clone materially unreproducible (D-03, P2, repository-scope, disclosed).

## Audit Scope and Coverage

- **Agreed scope:** All 26 sections of the audit brief (base/diff, registry/profiles, orchestrator security, conformance, describe/doctor, all profiles, build/placeholder, ESLint/a11y gaps, assets, gitleaks, dependencies, evidence/migration advisories, issue + ship governance, GitHub workflow, objective/subjective boundary, docs).
- **Evidence inspected:** Full adapter source, config, docs, impl log, normative spec + repo baseline; direct execution of all six commands, the conformance suite, the production build, npm audit, gitleaks (incl. planted-secret fixtures), and the exported registry/governance functions.
- **Coverage completed:** Every review area below has a disposition row.
- **Coverage not completed:** Live GitHub Actions run (static + local-equivalent only); signal-driven exit 5 under synchronous `spawnSync` (best-effort handler, not stress-tested); frozen-art byte proofs and art-dependent living-atlas tests (require git-ignored art).

## Defects Found

| ID | Severity | Defect | Evidence | Closure impact | Status |
|---|---|---|---|---|---|
| D-01 | medium (P2) | `eval-evidence`, `dep-audit`, `migration-ledger` are advisory though the Manydoors baseline (audit sec 18) prescribes them blocking; eval-evidence + dep-audit currently fail on pre-existing debt, so advisory keeps ship green; no blocking regression gate in reliability/SCA/generated-consistency | full/ship logs show `[ADVISORY-FINDINGS]` for eval-evidence (#32) and dep-audit (7 moderate); baseline sec 18 blocking list | Does not block this change; weakens durable gate | open |
| D-02 | medium (P2) | Standard sec 12 requires generated-artifact inconsistency to block; no blocking check verifies schema-vs-migration drift (deferred to manual `db:generate`), and the deterministic ledger-integrity check that IS feasible is advisory | `checkMigrationDrift` proven to detect drift deterministically yet classified advisory; docs "Deferred and manual" | Schema drift could ship undetected | open |
| D-03 | medium (P2) | Production art (`masters`/`masks`/`.ora`) is git-ignored; a bare clone cannot reproduce the full product, and frozen-art byte-equality proofs cannot run in the gate | `git status --ignored`; docs "Asset assurance" section; map-art-proof reports missing working art | Repository/release-scope; correctly disclosed by adapter | accepted |
| D-04 | low (P3) | Real check timeouts throw -> exit 3, but header/describe/spec label 5="timed out"; conformance asserts timeout->5 via a stub no real check uses | `verify.mjs` run functions `throw` on `r.timedOut`; test line uses `{timeout:true}` stub | Fail-closed preserved (both non-zero); mislabels timeouts | open |
| D-05 | low (P3) | dep-audit docs say advisories are "next/postcss chain ... unfixable without a breaking downgrade"; actually 3 chains incl. production `@anthropic-ai/sdk` (GHSA-p7fg) and dev `drizzle-kit/esbuild`; sdk fix is a forward major, not a downgrade | `npm audit --json`; docs/verification.md sec "Security tooling" | Check prints full truth; narrative understates | open |
| D-06 | low (P3) | `ship-issue-ref` accepts any `#<n>` (commit-message format gate), not issue existence/linkage; secret-scan is working-tree-only (`--no-git`), a disclosed committed-history blind spot | `verify.mjs checkShipIssueRef` regex `/#[0-9]+/`; gitleaks args | Documented scope; #232 verified real/correct | accepted |
| D-07 | low (P3) | ESLint/Prettier and browser/a11y automation deferred; map-art-proof "22-locale reconciliation (blocking)" overstates (structural art-pack errors exit 0; count derived not asserted); CI Node 22 vs docs "20+"; actions pinned to `@v4` tags not SHAs | baseline sec 12; map-art-proof `{breakpoints:[]}` -> exit 0; ci.yml | Truthfully documented; consistent with standard sec 1.6 | accepted |

## Corrections Applied

- None; audit was read-only.

## Review Area Disposition

| Review area | Expected behavior or criterion | Evidence | Result | Residual limitation |
|---|---|---|---|---|
| Canonical base / ancestry | base unmoved, ancestor of HEAD | `origin/main==e06db1f`, `--is-ancestor` YES | VERIFIED | None |
| Diff purity | only 8 governance files, no product change | `git diff --name-status`; lock unchanged | VERIFIED | None |
| Registry + profiles | 17 checks; 6/11/17/7; subset relations | `describe --json`, `_plan`, conformance | VERIFIED | None |
| Incomplete-run safety | executed==planned; incomplete/dup -> 3 not 0 | engine code + conformance tests | VERIFIED | None |
| Orchestrator security | no shell, stdin ignored, no destructive git, no secret log | source + conformance tests 34-37 | VERIFIED | None |
| Conformance quality | real engine, temp git repos, no fake-success | verify.test.mjs 23/23 over `runProfile` | VERIFIED | timeout->5 stub (D-04) |
| describe / doctor | read-only, no install/start, truthful readiness | direct run; exit 0; PATH-stub test | VERIFIED | None |
| fast / github-minimal / full / ship | run exactly once, correct exit codes | direct runs; ship 14 pass exit 0 | VERIFIED | None |
| Build + placeholder key | offline build; key not in bundle | build exit 0; `.next` grep clean | VERIFIED | None |
| gitleaks allowlist | narrow, fail-closed | planted-secret fixtures; missing-bin probe | VERIFIED | working-tree only (D-06) |
| Dependency advisories | truthful; no exploitable prod vuln hidden | npm audit; sdk memory-tool unused | VERIFIED | classification (D-01/D-05) |
| eval-evidence / migration-ledger | execute, report, visible, deterministic | full/ship logs; drift fixture | VERIFIED | advisory vs baseline (D-01/D-02) |
| Issue + ship governance | #232 real under #30; branch/base/clean/log gates | gh; ship governance passes; conformance | VERIFIED | format-only issue ref (D-06) |
| GitHub workflow | npm ci, github-minimal only, least-privilege, no deploy | ci.yml read; actionlint exit 0 | VERIFIED | not run on GitHub |
| Objective/subjective boundary | no automated subjective/a11y claim | docs + describe | VERIFIED | None |
| Generated-consistency (sec 12) | inconsistency MUST block | no blocking drift check | FAILED | D-02 |
| Reproducibility of release assets | bare clone can build/run product | git-ignored art | NOT VERIFIED | D-03 |

## Validation Evidence

| Command or Procedure | Result | Evidence | Proves | Does Not Prove |
|---|---|---|---|---|
| `node --test scripts/verify.test.mjs` | PASS 23/23 | VERIFIED | dispatch, exit engine, precedence, no-drift, stdin, ship governance | heavy real checks |
| `./scripts/verify ship --base origin/main` | exit 0 | VERIFIED | 14 blocking pass, 3 advisory, 17/17 reconciled | future regressions in advisory domains |
| `./scripts/verify fast|full|github-minimal` | exit 0 (6/6, 8+3, 7/7) | VERIFIED | offline gate, build, secret scan | frozen-art proofs |
| `next build --webpack` + `grep .next` | PASS; placeholder absent | VERIFIED | offline build; no key leak | runtime behavior |
| gitleaks planted-secret fixtures | detect exit 1; allowlist narrow | VERIFIED | fail-closed; no over-suppression | full git history |
| `depAvailable('gitleaks'/'npm')` with tool off PATH | false | VERIFIED | fail-closed exit-4 path | live signal->5 |
| real `checkMigrationDrift` on corrupted ledger | ok:false | VERIFIED | deterministic drift detection | schema-vs-migration drift |
| `npm audit --json` | 7 moderate | VERIFIED | all low/unreachable in-app | zero risk |
| `gh issue view 232 / 30`; actionlint | #232 OPEN under #30; exit 0 | VERIFIED | issue governance; workflow well-formed | live CI run |

## Final Validation Summary

- **Overall validation:** PASS
- **Test files:** 1
- **Tests passed:** 23
- **Tests failed:** 0
- **Tests skipped:** 0
- **Other gates:** conformance 23/23 (`scripts/verify.test.mjs`, 1 file). unit-tests ran 127 src test files (1222 cases per impl log, SESSION EVIDENCE), all passing; 16 `scripts/living-atlas/*.test.ts` excluded (git-ignored art). eval-evidence sub-check #32 fails by design (advisory). typecheck PASS; build PASS; secret-scan PASS (fail-closed); actionlint PASS
- **Checks not run:** live GitHub Actions run; frozen-art byte proofs; signal-driven exit 5 stress test
- **Confidence supported:** adapter is deterministic, drift-free registry/execution, fail-closed on secrets and missing tools, no bundle leak, sound ship governance; clean HEAD ships green legitimately
- **Does not prove:** any subjective/visual/kid-safety property (out of scope); that advisory domains gate future regressions; full-history secret cleanliness; live CI green on GitHub

## Accepted Limitations

- D-03 (git-ignored production art) and D-06/D-07 items are pre-existing repository properties or standard-consistent deferrals, truthfully disclosed by the adapter; they do not indicate an adapter defect or a false success. They do not, by themselves, make this specific clean shipment unsafe.

## Remaining Findings

- D-01 and D-02 (P2) are open: the advisory-vs-baseline-blocking classifications and the absent blocking generated-consistency gate should be refined before the adapter is accepted as the durable authoritative gate.
- D-04, D-05 (P3) open: timeout exit-code labeling/coverage and dep-audit doc accuracy.

## Final Recommendation

- **Recommendation:** Continue remediation (refinement, not redesign)
- **Ready to finalize:** conditional
- **Issue disposition:** Issue #232 remained open.
- **Rationale:** The adapter is safe, honest, deterministic, and close to ready; this clean shipment is green with no P0/P1. Before durable acceptance, make eval-evidence and the deterministic migration-ledger blocking-with-a-narrow-documented-waiver so regressions block while known pre-existing debt is explicitly waived, decide dep-audit blocking-on-new-advisories vs tracked advisory, correct the dep-audit docs, and record the git-ignored-art reproducibility gap as a tracked repository concern.

## Next Action

Report CHANGES_REQUIRED to the commissioner with D-01/D-02 as the blocking P2 items and D-03 as a disclosed repository-scope concern; do not merge, push, or deploy.

## Shareable Summary

- **Audit verdict:** CHANGES REQUIRED
- **Defects found and corrected:** 7 findings (0 P0, 0 P1, 3 P2, 4 P3); 0 corrected (read-only audit)
- **Review-area coverage:** all requested areas dispositioned; 2 marked FAILED/NOT VERIFIED (generated-consistency sec 12; bare-clone asset reproducibility), rest VERIFIED
- **Validation:** ship/full/fast/github-minimal all exit 0; conformance 23/23; 127 src test files; build PASS; gitleaks fail-closed + narrow allowlist; actionlint PASS
- **Accepted limitations:** git-ignored production art and standard-consistent lint/a11y deferrals, all truthfully disclosed
- **Remaining findings:** D-01 (advisory checks the baseline prescribes blocking leave no regression gate), D-02 (no blocking generated-consistency gate, sec 12), plus P3 timeout-label and doc-accuracy items
- **Issue state:** open
- **Next action:** refine advisory classifications toward blocking-with-known-waiver; correct dep-audit docs; track the art reproducibility gap; then re-audit

The Manydoors V1 adapter is a genuinely strong first adoption: one authoritative registry drives describe and execution with no drift, the engine reconciles executed-vs-planned so an incomplete or duplicate-id run can never return 0, child processes are argv-array + stdin-isolated with only read-only git, the synthetic build key never reaches any bundle, the gitleaks scan is fail-closed with a verifiably narrow allowlist, ship governance (branch/base/merge-base/clean-tree/native-#232/impl-log) is sound, and it adds the repository's first CI. The clean committed HEAD ships green (ship exit 0, 14/14 blocking, 3 advisory, 17/17 reconciled) and no P0/P1 exists. It is not yet READY only because three checks the repository's own baseline prescribes blocking - evidence-integrity, SCA, and Drizzle migration-drift - are wholesale-advisory (two currently red on pre-existing debt), which keeps ship green but leaves reliability, dependency-vulnerability, and generated-consistency (standard section 12) with no blocking regression gate; and because git-ignored production art makes a bare clone unable to reproduce the full product. These are refinements a reviewer should require before the adapter becomes the durable gate; none makes this specific shipment unsafe. Active checkout, Fidem, Hachira, DotFiles standards, and /audit-and-pr were untouched; no commit, push, PR, merge, or deploy occurred.
