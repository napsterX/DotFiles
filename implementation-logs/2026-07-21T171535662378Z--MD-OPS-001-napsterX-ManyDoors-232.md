---
schema_version: 2
log_id: ilog-20260721T171237688196Z-af6ebd94
created_at: 2026-07-21T17:12:37Z
task_type: audit
status: completed
task_id: MD-OPS-001 (napsterX/ManyDoors#232)
session_id: 2f48ce68-d87b-449c-833d-75b08b9731bd
prompt_id: d13d95de-4b2e-4196-917f-dc3d8057630e
repository: /Users/manish/git/DotFiles
branch: master
head_before: acd2da51e0d3299a3d75cdbb8078f5e7e453019b
head_after: acd2da51e0d3299a3d75cdbb8078f5e7e453019b
validation_status: passed
issue_state: open
audit_verdict: changes_required
---

# Implementation Log: Final re-audit of the remediated Manydoors Repository Verification V1 adapter

## Objective

Independently determine whether the remediated Manydoors `./scripts/verify` adapter (Repository Verification Standard V1) is genuinely ready for shipment, by reproducing every remediation claim rather than trusting the remediation report.
Decision condition: READY only if the canonical base is unchanged, no blocking P0/P1/P2 finding survives, baselines are narrow and regression-blocking, generated checks leave the real repo untouched, runtime art reconciles and hash-matches, timeouts map to exit 5, all 19 ship checks execute exactly once, and final ship returns exit 0.

## Scope

### Included

- Repository safety and canonical-base ancestry; diff purity; registry and profile derivation; eval-evidence baseline; dependency baseline; migration-ledger; generated-consistency; art provenance and release-art; map-art structural validation; timeout/signal handling; process and temp-file security; conformance-test quality; describe/doctor; direct fast/github-minimal/full/ship execution; build and placeholder isolation; secret scan; CI workflow; issue and documentation accuracy.

### Excluded

- Any modification to the audited repository, Fidem, Hachira, the DotFiles standards, or `/audit-and-pr`. No commit, push, PR, merge, deploy, or issue-state change. Subjective/kid-safety/visual/accessibility properties (out of scope by design).

## Repository and Task State

- **Repository state:** This log's host repo (DotFiles) is clean at `acd2da5` (unchanged). Audited worktree `/private/tmp/manydoors-repository-verification-v1` @ `c6e70a3`, tracked-clean. Active checkout `/Users/manish/git/ManyDoors` @ `e06db1f` byte-identical to its pre-audit snapshot (unchanged).
- **Branch:** audited branch `feat/repository-verification-v1` (2 commits: `c5ee98b` adapter, `c6e70a3` remediation) atop `origin/main @ e06db1f`.
- **HEAD:** audited HEAD `c6e70a38b2c5f7462de507574a93f04d8060b815`.
- **Task/issue:** MD-OPS-001, napsterX/ManyDoors#232; follow-ups #233 (source-master provenance), #234 (retire accepted baselines).
- **Issue state:** open
- **Issue action:** none (read-only audit).

## Work Performed

- **VERIFIED** — Fetched `origin` safely: `origin/main` still `e06db1f` (merge-base `e06db1f`); canonical base has not moved, so full read-only validation proceeded.
- **VERIFIED** — Parsed `CHECK_REGISTRY` directly from source: 19 checks, all `status: blocking`, 0 advisory; independently derived fast=6, full=13, ship=19, github-minimal=9; verified `fast ⊂ full ⊂ ship`, `github-minimal ⊆ full`, no duplicate ids, one run fn each. `_plan` output for each profile reconciles ID-for-ID.
- **VERIFIED** — Ran conformance suite (`node --test scripts/verify.test.mjs`): 44/44 pass, exit 0. Independently re-derived eval-evidence/dep-audit/release-art/migration-ledger verdicts from imported production functions.
- **VERIFIED** — Executed all profiles on the clean committed HEAD: fast 6/6, github-minimal 9/9, full 13/13, ship 19/19, each check executed exactly once, all exit 0; final ship summary `planned=19 executed=19 pass=19 fail=0 unavailable=0 advisory=0 skipped=0`.
- **VERIFIED** — Live `npm audit` reconciles exactly with `.verify/dependency-baseline.json` (3 moderate advisories: sdk/prod, postcss/prod, esbuild/dev). Fixtures confirm new/severity-up/dev→prod/stale/critical all block.
- **VERIFIED** — 52 provenance assets == 52 tracked `public/atlas` files (unique paths/hashes); generated-consistency leaves the real tree byte-identical and cleans its temp dir; map-art structural fixtures (11 cases) fail closed while absent source-masters stay non-structural; real `sleep` timeout → exit 5, child SIGTERM-terminated, no orphan; planted secret blocks (exit 1); build placeholder absent from `.next`.

## Files Changed or Inspected

| Path | State | Purpose |
|---|---|---|
| `scripts/verify.mjs` | inspected | Registry, engine, all check verdict functions |
| `scripts/verify.test.mjs` | inspected | Conformance coverage and rmSync/temp guards |
| `src/cli/map-art-proof.ts` | inspected | Structural exit-1 change (diff confined to truthful exit) |
| `src/cli/eval-evidence-verify.ts` | inspected | 33-sub-check validator; doc-ref aggregation (D-01 basis) |
| `.verify/eval-evidence-baseline.json` | inspected | Accepted-finding baseline (weakness found) |
| `.verify/dependency-baseline.json` | inspected | Accepted-advisory baseline (reconciled to live npm audit) |
| `art-provenance.json` | inspected | 52 runtime assets + source-master provenance |
| `docs/verification.md`, `.github/workflows/ci.yml`, `.gitleaks.toml`, `package.json` | inspected | Docs, CI backstop, secret-scan config, npm aliases |

## Decisions and Invariants

- **VERIFIED** — Registry-driven single source of truth holds: describe/describe --json/doctor/execution derive from one 19-check array; planned==executed reconciliation prevents an incomplete run returning 0; exit precedence 3>5>1>4>0.
- **VERIFIED** — generated-consistency is read-only wrt the repo: copies `drizzle/` into an OS `mkdtemp`, runs `drizzle-kit generate` into the temp copy, `rmSync` targets only `tmp`; real worktree fingerprint identical before/after; 0 leftover `md-verify-drizzle-` dirs.
- **VERIFIED** — Diff is pure: 13 files, all verification-scoped; `package-lock.json` unchanged; `scripts/verify` is mode 100755; no product/schema/migration/deploy behavior changed; map-art edit and package.json change (6 `verify:*` aliases) are additive/truthful.
- **VERIFIED** — dep-audit matches per exact GHSA id (narrow, regression-blocking); high/critical never baselineable.

## Audit Verdict

- **Verdict:** CHANGES REQUIRED
- **Decision basis:** One blocking P2 baseline weakness: the eval-evidence accepted-finding baseline cannot detect an additional unresolved documentation evidence reference, so a new broken `evals/evidence/...` citation would be silently accepted (brief Section 7 case 2 requires exit 1). No P0/P1. All other claims reproduced true.

## Audit Scope and Coverage

- **Agreed scope:** Prove every remediation claim (19 blocking checks, 0 advisory; exact baselines regression-blocking; migration-ledger and generated-consistency blocking; 52 hash-pinned assets; map-art fail-closed; timeout→5; ship 19×1 → exit 0).
- **Evidence inspected:** V1 specification, Manydoors baseline, adapter + tests + validators, baselines, provenance, docs, CI, gitleaks config, both implementation logs, issue state; live profile/conformance/npm-audit/gitleaks/drizzle-kit runs.
- **Coverage completed:** All 17 review areas exercised with direct execution or reproduced fixtures.
- **Coverage not completed:** Frozen-art byte proofs over source masters (absent by design, art-production only, #233) not run; subjective/visual/kid-safety properties out of scope.

## Defects Found

| ID | Severity | Defect | Evidence | Closure impact | Status |
|---|---|---|---|---|---|
| D-01 | medium (P2) | eval-evidence baseline suppresses an *additional* unresolved doc evidence reference: the validator aggregates all unresolved `evals/evidence/...` citations into one sub-check `documentation evidence references resolve`; the baseline matches by name + substring, so a joined detail of the accepted substring plus a new unresolved ref still matches and passes. | `evalEvidenceVerdict` (`scripts/verify.mjs:254-267`) + aggregated sub-check (`src/cli/eval-evidence-verify.ts:268-279`); reproduced: verdict `ok=true` on accepted+new detail (brief expects exit 1). Contradicts the "never suppresses new references" claim in `scripts/verify.mjs:227-233` and the baseline `note`. | Blocks closure (defeats stated regression guarantee for that sub-check via a plausible doc edit) | open |
| D-02 | low (P3) | migration-ledger does not block duplicate journal identifiers or invalid/non-monotonic ordering. | `migrationLedgerVerdict` (`scripts/verify.mjs:412-430`); fixtures: duplicate tag and idx `[0,2]`/`[1,0]` return `ok=true` (brief lists as expect-block). Orphan/unregistered/missing-snapshot ARE caught; malformed journal → exit 3. | Does not block (machine-generated journal; generated-consistency covers real schema drift; docs scope the check honestly) | open |
| D-03 | low (P3) | Conformance test helpers (`newRepo`, `tmpDrizzle`, drift/mapart) never clean their OS temp dirs; they accumulate across runs. | 54 `md-verify-repo-*`, 9 `md-verify-bin-*`, plus ledger/drift/mapart dirs left in `$TMPDIR`; production generated-consistency leaves 0. | Does not block (test hygiene only; harmless OS-temp pollution) | open |

## Corrections Applied

- None; audit was read-only.

## Review Area Disposition

| Review area | Expected behavior or criterion | Evidence | Result | Residual limitation |
|---|---|---|---|---|
| Canonical base / safety | origin/main unchanged; active checkout untouched | `origin/main`=`e06db1f`; active checkout byte-identical | VERIFIED | None |
| Diff purity | Only verification-scoped changes; lockfile stable | 13 files; `package-lock.json` absent from diff; map-art edit truthful | VERIFIED | None |
| Registry & profiles | 19 blocking, 0 advisory; 6/13/19/9; subset laws | Source parse + `_plan` + describe --json | VERIFIED | None |
| Eval-evidence baseline | New/changed/stale finding blocks; narrow | Fixtures 1,2b,3,4 correct; **case 2 (added ref) passes** | FAILED | D-01 (P2) |
| Dependency baseline | New/severity/dev→prod/stale/critical block; matches live audit | Live npm audit == baseline; fixtures all block | VERIFIED | None |
| Migration-ledger (blocking) | orphan/missing/dup/order/malformed handled | orphan/missing/malformed handled; dup/order not | CORRECTED?→PARTIAL | D-02 (P3) |
| Generated-consistency | drizzle-kit generate in temp; real tree unchanged; drift blocks | Fingerprint identical; drift fixture exit 1; temp cleaned | VERIFIED | None |
| Art classification & provenance | 52 tracked+pinned+hash-matched; masters absent-by-design | 52==52; hashes match in full run; #233 provenance | VERIFIED | None |
| Release-art check | missing/untracked/hash/unpinned/dup block | 6 injected-io fixtures all correct; real full run PASS | VERIFIED | None |
| Map-art structural | structural defects exit 1; absence/approval non-structural | 11 fixtures correct; clean checkout exit 0 | VERIFIED | None |
| Timeout & signal | real timeout → exit 5; child terminated; no orphan | `run('sleep')` timedOut+SIGTERM; profile exit 5; 0 orphans | VERIFIED | None |
| Process & temp security | no shell/eval; rmSync temp-only; no destructive git | argv spawn; stdin ignore; rmSync→`tmp`; guards asserted | VERIFIED | None |
| Conformance quality | drives real production logic; 44 tests | 44/44 pass; imports real verdict fns + real spawn | VERIFIED | D-03 (P3, hygiene) |
| Describe & doctor | read-only; 19/0; 6/13/19/9; truthful | exit 0; counts match; no secret leak | VERIFIED | None |
| Profile execution | fast/ghmin/full/ship exit 0; ship 19×1 | 6/9/13/19 executed once each; ship exit 0 | VERIFIED | None |
| Build/secret/CI | build w/ synthetic key; placeholder absent; gitleaks fail-closed; actionlint | `.next` placeholder count 0; planted secret exit 1; actionlint exit 0 | VERIFIED | None |
| Issues & docs | #232/#233/#234 open; docs accurate | all OPEN; docs match behavior except D-01 claim | VERIFIED (minor doc claim off, see D-01) | D-01 note |

## Validation Evidence

| Command or Procedure | Result | Evidence | Proves | Does Not Prove |
|---|---|---|---|---|
| `node --test scripts/verify.test.mjs` | PASS 44/44 | VERIFIED | conformance over real engine/verdicts/spawn | doc-ref aggregation blind spot (D-01) |
| `./scripts/verify fast|github-minimal|full|ship --base origin/main` | exit 0 (6/9/13/19) | VERIFIED | each check runs once; ship green | subjective properties |
| `npm audit --json` / `--omit=dev` | 3 adv, matches baseline | VERIFIED | dependency baseline current & narrow | future advisories |
| eval-evidence/dep-audit/release-art/migration-ledger fixtures | see D-01/D-02 | VERIFIED | regression-blocking except D-01/D-02 | - |
| generated-consistency real-tree fingerprint | identical before/after | VERIFIED | read-only wrt repo; temp cleaned | - |
| map-art 11 structural fixtures; real timeout; planted gitleaks | as expected | VERIFIED | fail-closed structure; exit 5; secret blocks | - |
| `actionlint .github/workflows/ci.yml`; `bash -n`; `node --check` | PASS / exit 0 | VERIFIED | CI + launcher + adapter well-formed | - |

## Final Validation Summary

- **Overall validation:** PASS
- **Test files:** 1
- **Tests passed:** 44
- **Tests failed:** 0
- **Tests skipped:** 0
- **Note:** all gates green; one independent P2 finding (D-01) is not caught by any gate and drives the CHANGES_REQUIRED verdict.
- **Other gates:** typecheck PASS; next build PASS; migration-ledger PASS; generated-consistency PASS; gitleaks exit 0; actionlint exit 0.
- **Checks not run:** frozen-art byte proofs over source masters (#233); subjective/visual/kid-safety (by design).
- **Confidence supported:** the 19-check gate is complete, blocking, registry-consistent, read-only wrt the repo, and returns exit 0 on the clean HEAD; art/secret/build/timeout/generated integrity all reproduced.
- **Does not prove:** that the eval-evidence baseline blocks a newly-added unresolved documentation citation (it does not, D-01).

## Accepted Limitations

- Absent editable source masters (art-production environment, #233) are absent by design; the runtime release derivatives under `public/atlas` are fully tracked and hash-pinned, so a clean clone builds/runs the product. Truthfully represented; never reported as a passed frozen-art proof.
- Three moderate advisories and the doc-ref citation are accepted, narrow, and tracked for retirement in #234.

## Remaining Findings

- D-01 (P2, blocking): eval-evidence baseline fails to block an additional unresolved documentation evidence reference. Correct by matching the doc-ref finding on exact full detail or by keying each unresolved reference separately (or emit one sub-check per reference in the validator).
- D-02 (P3): add duplicate-tag and monotonic-idx checks to `migrationLedgerVerdict`.
- D-03 (P3): add test teardown to remove conformance temp dirs.

## Final Recommendation

- **Recommendation:** Continue remediation (address D-01; D-02/D-03 optional hardening).
- **Ready to finalize:** no.
- **Issue disposition:** #232/#233/#234 remained open (unchanged).
- **Rationale:** A single, well-scoped P2 defeats the stated regression-blocking guarantee of the eval-evidence baseline for the doc-reference sub-check and contradicts an explicit in-code/in-baseline claim; the brief's READY criteria require all baselines to be narrow and regression-blocking. Everything else reproduced true.

## Next Action

Return CHANGES_REQUIRED to the shipment flow; fix D-01 (narrow the eval-evidence doc-ref match), re-run `full`/`ship`, and re-audit that one check. No push/PR/merge/deploy.

## Shareable Summary

- **Audit verdict:** CHANGES REQUIRED
- **Defects found and corrected:** 3 found (1 P2 blocking, 2 P3), 0 corrected (read-only audit).
- **Review-area coverage:** all 17 requested areas reviewed; 15 VERIFIED, 1 FAILED (eval-evidence baseline), 1 PARTIAL (migration-ledger).
- **Validation:** conformance 44/44 pass; fast/github-minimal/full/ship exit 0 (6/9/13/19 checks, each once); npm audit == baseline; actionlint/gitleaks/build all green.
- **Accepted limitations:** absent source masters (#233); three moderate advisories + doc-ref citation (#234) - narrow and tracked.
- **Remaining findings:** D-01 (P2) eval-evidence baseline cannot block an added unresolved doc reference; D-02/D-03 (P3) hardening.
- **Issue state:** open (unchanged).
- **Next action:** fix D-01, re-run full/ship, re-audit the eval-evidence check.

The remediated adapter is strong: 19 blocking checks with 0 advisory, a registry-consistent engine that cannot return 0 on an incomplete run, generated/release-art/secret/build/timeout integrity all independently reproduced, the dependency baseline reconciled to live `npm audit`, 52 runtime assets tracked and hash-matched, map-art failing closed on structural defects, and a clean, pure diff that leaves the active checkout and canonical base untouched.
It is not yet READY only because of D-01: the eval-evidence check aggregates all unresolved documentation evidence references into a single sub-check, and the accepted-finding baseline matches that sub-check by a substring, so an additional broken `evals/evidence/...` citation added to either scanned doc would be silently accepted (exit 0) when the brief requires exit 1 - and the code/baseline affirmatively claim this cannot happen. Narrowing the doc-ref match (exact detail, or per-reference keys) closes it. No P0/P1; no repository, standard, or sibling repo was modified; no commit, push, PR, merge, or deploy occurred.
